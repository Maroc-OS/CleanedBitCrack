language: cpp
sudo: required
dist: bionic
compiler:
- clang
matrix:
  include:
    # works on Precise and Trusty
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-bionic-7
          packages:
            - gcc
      env:
        - MATRIX_EVAL="CC=gcc && CXX=g++"

    - os: osx
      osx_image: xcode10.1
      env:
        - MATRIX_EVAL=""

before_install:
    - eval "${MATRIX_EVAL}"
    - make clean
script:
    - make all
after_success:
    - ./bin/clunittest
    - ./bin/clBitCrack 173M3yw1P1SfiPiDHABLGi5LLBAMohVQ7h
